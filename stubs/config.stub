{{{
  exports({ to: app.configPath('geolite2.ts') })
}}}
import { defineConfig } from '@stouder-io/adonis-geolite2'

export default defineConfig({
  downloadDir: path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', 'dbs')
})
